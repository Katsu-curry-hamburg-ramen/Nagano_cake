<table class="table table-hover table-bordered">
  <thead>
    <tr>
      <th class="active text-center">ジャンル検索</th>
    </tr>
  </thead>
  <tbody>
    <% @genres.each do |genre| %>
      <tr>
        <td><%= link_to genre.genrename, search_public_items_path(genre_name: genre.genrename), :style=>"color:black" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
